[tool.poetry]
name = "workflow-create-subtraction"
version = "0.1.0"
description = "A workflow for creating subtractions in Virtool."
authors = ["Tiansheng Sui, Ian Boyes, Blake Smith"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
virtool-workflow = "^0.6.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
